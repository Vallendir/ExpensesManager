apply plugin: 'org.springframework.boot'

dependencies {
    implementation "org.springframework.data:spring-data-mongodb"

    implementation project(':core')
    implementation project(':exception')
}

bootJar {
    mainClassName = 'pl.em.RESTAppRunner'
}